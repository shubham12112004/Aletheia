{
  "name": "ai-investment-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix Investment-main run dev",
    "server": "npm --prefix Investment-main run server",
    "build": "npm --prefix Investment-main run build",
    "typecheck": "npm --prefix Investment-main run typecheck"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
